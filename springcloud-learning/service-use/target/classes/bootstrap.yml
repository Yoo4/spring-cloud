spring:
  cloud:
    config:
      uri: http://localhost:8002/
#      discovery:                          #无法通过服务名调取服务，只能通过地址来
#        service-id: server-config
#        enabled: true
      label: master  #git分支
      name: uc
      profile: dev
